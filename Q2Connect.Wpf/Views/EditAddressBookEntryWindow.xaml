<Window x:Class="Q2Connect.Wpf.Views.EditAddressBookEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Address Book Entry" Height="235" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Address -->
        <TextBlock Grid.Row="0" Text="Address:" VerticalAlignment="Center" Margin="0,0,0,8"/>
        <TextBox Grid.Row="1" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" 
                 Height="25" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

        <!-- Label -->
        <TextBlock Grid.Row="2" Text="Label:" VerticalAlignment="Center" Margin="0,0,0,8"/>
        <TextBox Grid.Row="3" Text="{Binding Label, UpdateSourceTrigger=PropertyChanged}" 
                 Height="25" VerticalContentAlignment="Center" Margin="0,0,0,15"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Height="30" Margin="0,0,10,0" 
                    IsDefault="True" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="80" Height="30" 
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

