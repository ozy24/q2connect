<Window x:Class="Q2Connect.Wpf.Views.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="About" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Application Name and Version -->
        <TextBlock Grid.Row="0" x:Name="VersionTextBlock"
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <!-- Author -->
        <TextBlock Grid.Row="1" Text="by ozy" 
                   FontSize="14" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <!-- GitHub Link -->
        <TextBlock Grid.Row="2" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20">
            <Hyperlink NavigateUri="https://github.com/ozy24/q2browser" RequestNavigate="Hyperlink_RequestNavigate">
                <Run Text="https://github.com/ozy24/q2browser"/>
            </Hyperlink>
        </TextBlock>

        <!-- Close Button -->
        <Button Grid.Row="4" Content="Close" 
                Width="80" Height="30" 
                HorizontalAlignment="Center"
                IsDefault="True" IsCancel="True"
                Click="CloseButton_Click"/>
    </Grid>
</Window>


